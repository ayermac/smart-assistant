---
phase: "01-foundation-and-cli-skeleton"
plan: "01-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - src/index.ts
  - src/config.ts
  - .gitignore
autonomous: true
requirements:
  - FND-01
user_setup: []
must_haves:
  truths:
    - "Project has package scripts named dev, build, typecheck, and start."
    - "Project is configured as an ESM TypeScript app for Node >=22.19.0."
    - "Local data directory defaults are centralized in src/config.ts."
  artifacts:
    - package.json
    - tsconfig.json
    - src/index.ts
    - src/config.ts
    - .gitignore
  key_links:
    - "package.json build and typecheck scripts both use tsconfig.json."
    - "src/index.ts exports the configuration helpers from src/config.ts."
---

<objective>
Create the TypeScript project scaffold for `smart-assistant`.

Purpose: Establish the runnable foundation that later phases can build on without deciding agent runtime details too early.
Output: `package.json`, `tsconfig.json`, initial `src/` files, and ignore rules for generated/local data.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/01-foundation-and-cli-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-and-cli-skeleton/01-VALIDATION.md
@README.md
</context>

<threat_model>
ASVS Level: 1
Block on: high severity threats.

- T-01-01: Local secrets or generated data are accidentally committed. Mitigation: `.gitignore` must include `.env` and `.smart-assistant/`; `.env.example` is handled separately without secrets.
- T-01-02: Phase 1 adds unnecessary runtime dependencies before the agent integration design is ready. Mitigation: `package.json` only adds TypeScript development dependencies; `pi-ai` and `pi-agent-core` are deferred to Phase 2.
- T-01-03: Node/runtime drift creates later integration failures with `pi`. Mitigation: `engines.node` must be `>=22.19.0`, matching the local `pi` reference project.

Verification references: Task 1 package checks, Task 3 `.gitignore` checks, and `01-VALIDATION.md` command-based validation.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create package metadata and scripts</name>
  <files>package.json</files>
  <read_first>README.md, .planning/PROJECT.md, .planning/phases/01-foundation-and-cli-skeleton/01-RESEARCH.md</read_first>
  <action>Create `package.json` for a private ESM TypeScript CLI app. Use exact values: `name` = `smart-assistant`, `version` = `0.1.0`, `private` = `true`, `type` = `module`, `bin.smart-assistant` = `./dist/cli.js`, `engines.node` = `>=22.19.0`. Add scripts: `dev` = `tsx src/cli.ts`, `build` = `tsc -p tsconfig.json`, `typecheck` = `tsc -p tsconfig.json --noEmit`, `start` = `node dist/cli.js`. Add devDependencies: `@types/node` `^24.12.4`, `tsx` `^4.22.1`, `typescript` `^5.9.3`. Do not add `@earendil-works/pi-ai` or `@earendil-works/pi-agent-core` yet; Phase 2 owns runtime integration.</action>
  <verify>node -e "const p=require('./package.json'); if(p.name!=='smart-assistant'||p.type!=='module'||!p.scripts.dev||!p.scripts.build||!p.scripts.typecheck||!p.scripts.start) process.exit(1)"</verify>
  <acceptance_criteria>
    - `package.json` contains `"name": "smart-assistant"`.
    - `package.json` contains `"type": "module"`.
    - `package.json` contains `"smart-assistant": "./dist/cli.js"`.
    - `package.json` contains `"dev": "tsx src/cli.ts"`.
    - `package.json` contains `"build": "tsc -p tsconfig.json"`.
    - `package.json` contains `"typecheck": "tsc -p tsconfig.json --noEmit"`.
    - `package.json` contains `"node": ">=22.19.0"`.
  </acceptance_criteria>
  <done>Package metadata and scripts are present with the exact values above.</done>
</task>

<task type="auto">
  <name>Task 2: Add strict Node ESM TypeScript configuration</name>
  <files>tsconfig.json</files>
  <read_first>package.json, .planning/phases/01-foundation-and-cli-skeleton/01-RESEARCH.md</read_first>
  <action>Create `tsconfig.json` with `compilerOptions.target` = `ES2022`, `module` = `NodeNext`, `moduleResolution` = `NodeNext`, `rootDir` = `src`, `outDir` = `dist`, `strict` = `true`, `esModuleInterop` = `true`, `forceConsistentCasingInFileNames` = `true`, `skipLibCheck` = `true`, `declaration` = `true`, and `sourceMap` = `true`. Set `include` to `["src/**/*.ts"]` and `exclude` to `["dist", "node_modules"]`.</action>
  <verify>node -e "const t=require('fs').readFileSync('tsconfig.json','utf8'); for (const s of ['\"module\": \"NodeNext\"','\"rootDir\": \"src\"','\"outDir\": \"dist\"','\"strict\": true']) if(!t.includes(s)) process.exit(1)"</verify>
  <acceptance_criteria>
    - `tsconfig.json` contains `"module": "NodeNext"`.
    - `tsconfig.json` contains `"moduleResolution": "NodeNext"`.
    - `tsconfig.json` contains `"rootDir": "src"`.
    - `tsconfig.json` contains `"outDir": "dist"`.
    - `tsconfig.json` contains `"strict": true`.
    - `tsconfig.json` includes `src/**/*.ts`.
  </acceptance_criteria>
  <done>TypeScript config supports strict Node ESM compilation from `src/` to `dist/`.</done>
</task>

<task type="auto">
  <name>Task 3: Create source layout and local data config</name>
  <files>src/index.ts, src/config.ts, .gitignore</files>
  <read_first>package.json, tsconfig.json, .planning/PROJECT.md, .planning/research/SUMMARY.md</read_first>
  <action>Create `src/config.ts` exporting `DEFAULT_DATA_DIR = \".smart-assistant\"`, `SMART_ASSISTANT_DATA_DIR_ENV = \"SMART_ASSISTANT_DATA_DIR\"`, `DATA_SUBDIRS = { sessions: \"sessions\", memory: \"memory\", knowledge: \"knowledge\", plans: \"plans\" }`, and a `resolveDataDir(env?: NodeJS.ProcessEnv): string` function returning `env?.SMART_ASSISTANT_DATA_DIR || DEFAULT_DATA_DIR`. Create `src/index.ts` exporting from `./config.js`. Create `.gitignore` with exact entries: `node_modules/`, `dist/`, `.env`, `.smart-assistant/`, and `*.log`.</action>
  <verify>test -f src/config.ts && test -f src/index.ts && test -f .gitignore && rg \"SMART_ASSISTANT_DATA_DIR|DEFAULT_DATA_DIR|DATA_SUBDIRS\" src/config.ts</verify>
  <acceptance_criteria>
    - `src/config.ts` contains `DEFAULT_DATA_DIR = ".smart-assistant"`.
    - `src/config.ts` contains `SMART_ASSISTANT_DATA_DIR`.
    - `src/config.ts` contains `sessions`, `memory`, `knowledge`, and `plans`.
    - `src/index.ts` contains `export * from "./config.js"`.
    - `.gitignore` contains `node_modules/`, `dist/`, `.env`, and `.smart-assistant/`.
  </acceptance_criteria>
  <done>Source layout and local data config are ready for later CLI and storage work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `package.json`, `tsconfig.json`, `src/index.ts`, `src/config.ts`, and `.gitignore` exist.
- [ ] `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8')); JSON.parse(require('fs').readFileSync('tsconfig.json','utf8'))"` exits 0.
- [ ] `rg "SMART_ASSISTANT_DATA_DIR|DEFAULT_DATA_DIR|DATA_SUBDIRS" src/config.ts` returns matches.
</verification>

<success_criteria>

- TypeScript project metadata is present.
- Build/typecheck scripts point at `tsconfig.json`.
- Local data defaults are centralized and exported.
- Generated dependencies and local data are ignored by git.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-cli-skeleton/01-01-SUMMARY.md`.
</output>
