---
phase: "01-foundation-and-cli-skeleton"
plan: "01-02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/cli.ts
  - src/index.ts
autonomous: true
requirements:
  - FND-02
user_setup: []
must_haves:
  truths:
    - "User can start the smart-assistant CLI from the terminal."
    - "CLI exposes --help and --version flags."
    - "CLI can enter and exit a minimal interactive prompt without model credentials."
  artifacts:
    - src/cli.ts
    - src/index.ts
    - dist/cli.js
  key_links:
    - "package.json dev script runs src/cli.ts."
    - "package.json bin points at dist/cli.js produced from src/cli.ts."
---

<objective>
Implement the minimal CLI command and startup flow.

Purpose: Give the project a runnable terminal entry point before the full agent runtime is wired in Phase 2.
Output: A TypeScript CLI that supports help/version flags, local data directory resolution, and a placeholder interactive loop.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/01-foundation-and-cli-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-and-cli-skeleton/01-VALIDATION.md
@.planning/phases/01-foundation-and-cli-skeleton/01-01-SUMMARY.md
@package.json
@tsconfig.json
@src/config.ts
@src/index.ts
</context>

<threat_model>
ASVS Level: 1
Block on: high severity threats.

- T-01-04: CLI accidentally calls model providers or external services before credentials and provider boundaries exist. Mitigation: Phase 1 CLI prints a placeholder response only and explicitly states that Phase 2 wires `pi-agent-core` and `pi-ai`.
- T-01-05: User input is executed as shell code or file paths. Mitigation: Phase 1 treats user input as plain text, supports only `/help` and `/exit`, and does not execute commands from the prompt.
- T-01-06: Unknown CLI flags fail silently. Mitigation: unknown options print `Unknown option: <flag>` and set `process.exitCode = 1`.

Verification references: Task 1 argument handling checks, Task 2 placeholder response checks, and `01-VALIDATION.md` CLI smoke checks.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement CLI argument handling</name>
  <files>src/cli.ts</files>
  <read_first>package.json, tsconfig.json, src/config.ts, .planning/phases/01-foundation-and-cli-skeleton/01-RESEARCH.md</read_first>
  <action>Create `src/cli.ts` with a shebang `#!/usr/bin/env node`. Import `readFile` from `node:fs/promises`, `createInterface` from `node:readline/promises`, `stdin` and `stdout` from `node:process`, and `resolveDataDir` from `./config.js`. Export an async `main(argv = process.argv.slice(2))` function. Implement exact flags: `--help`/`-h` prints usage containing `smart-assistant [--data-dir <path>]`, `--version`/`-v` reads `package.json` and prints its `version`, `--data-dir <path>` overrides `SMART_ASSISTANT_DATA_DIR`. Unknown flags print `Unknown option: <flag>` and set `process.exitCode = 1`.</action>
  <verify>test -f src/cli.ts && rg \"#!/usr/bin/env node|--help|--version|--data-dir|Unknown option\" src/cli.ts</verify>
  <acceptance_criteria>
    - `src/cli.ts` starts with `#!/usr/bin/env node`.
    - `src/cli.ts` contains `smart-assistant [--data-dir <path>]`.
    - `src/cli.ts` contains `--help`, `--version`, and `--data-dir`.
    - `src/cli.ts` contains `Unknown option:`.
    - `src/cli.ts` exports `async function main`.
  </acceptance_criteria>
  <done>CLI can parse help, version, data-dir, and unknown options deterministically.</done>
</task>

<task type="auto">
  <name>Task 2: Add placeholder interactive loop</name>
  <files>src/cli.ts</files>
  <read_first>src/cli.ts, src/config.ts, .planning/research/SUMMARY.md</read_first>
  <action>In `src/cli.ts`, implement a minimal interactive prompt using `readline/promises`. On startup print `smart-assistant local CLI` and `Data dir: <resolved path>`. Prompt with `you> `. Support `/help` by printing `Commands: /help, /exit`. Support `/exit` and empty EOF by closing the interface. For any non-command input, print exactly `assistant> Agent runtime is not connected yet. Phase 2 will wire pi-agent-core and pi-ai.` Do not call model providers in Phase 1.</action>
  <verify>rg \"smart-assistant local CLI|Data dir:|you>|/exit|Agent runtime is not connected yet\" src/cli.ts</verify>
  <acceptance_criteria>
    - `src/cli.ts` contains `smart-assistant local CLI`.
    - `src/cli.ts` contains `Data dir:`.
    - `src/cli.ts` contains `you>`.
    - `src/cli.ts` contains `Commands: /help, /exit`.
    - `src/cli.ts` contains `Agent runtime is not connected yet. Phase 2 will wire pi-agent-core and pi-ai.`
  </acceptance_criteria>
  <done>CLI can start, show data-dir context, accept input, and exit without model credentials.</done>
</task>

<task type="auto">
  <name>Task 3: Export CLI entry and verify runnable output</name>
  <files>src/index.ts, src/cli.ts</files>
  <read_first>src/index.ts, src/cli.ts, package.json, tsconfig.json</read_first>
  <action>Update `src/index.ts` to export `main` from `./cli.js` while preserving `export * from \"./config.js\"`. At the bottom of `src/cli.ts`, call `main().catch(...)` only when the file is executed directly; use an ESM-safe check comparing `import.meta.url` to `pathToFileURL(process.argv[1]).href`. Import `pathToFileURL` from `node:url`. In the catch handler, print the error message to stderr and set `process.exitCode = 1`.</action>
  <verify>rg \"export \\{ main \\} from \\\"./cli.js\\\"|pathToFileURL|process.exitCode = 1\" src/index.ts src/cli.ts</verify>
  <acceptance_criteria>
    - `src/index.ts` contains `export { main } from "./cli.js"`.
    - `src/index.ts` still contains `export * from "./config.js"`.
    - `src/cli.ts` contains `pathToFileURL`.
    - `src/cli.ts` contains `process.exitCode = 1`.
    - After dependencies are installed, `npm run build` creates `dist/cli.js`.
  </acceptance_criteria>
  <done>CLI is exported for future programmatic use and can be executed as a terminal command.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `src/cli.ts` contains help/version/data-dir handling.
- [ ] `src/index.ts` exports both config helpers and `main`.
- [ ] After dependencies are installed, `npm run typecheck` exits 0.
- [ ] After dependencies are installed, `npm run build` exits 0.
- [ ] After build, `node dist/cli.js --help` prints `smart-assistant [--data-dir <path>]`.
- [ ] After build, `node dist/cli.js --version` prints `0.1.0`.
</verification>

<success_criteria>

- CLI can be started from the terminal.
- CLI does not require model provider credentials in Phase 1.
- CLI communicates that the agent runtime arrives in Phase 2.
- CLI uses the local data directory configuration from Plan 01-01.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-cli-skeleton/01-02-SUMMARY.md`.
</output>
