---
phase: "01-foundation-and-cli-skeleton"
plan: "01-03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - README.md
  - .env.example
autonomous: true
requirements:
  - FND-03
  - FND-04
user_setup: []
must_haves:
  truths:
    - "README documents local setup commands."
    - "README documents local data directory behavior."
    - ".env.example documents Phase 1 environment variables."
    - "README and .planning remain the canonical planning surface; the legacy documentation directory is not recreated."
  artifacts:
    - README.md
    - .env.example
  key_links:
    - "README quick start commands match package.json scripts."
    - ".env.example variable names match src/config.ts."
---

<objective>
Document local setup, environment variables, and data directory conventions.

Purpose: Make the Phase 1 scaffold usable and keep planning artifacts aligned after the old documentation directory was removed.
Output: Updated README and `.env.example`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/01-foundation-and-cli-skeleton/01-RESEARCH.md
@.planning/phases/01-foundation-and-cli-skeleton/01-VALIDATION.md
@.planning/phases/01-foundation-and-cli-skeleton/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-cli-skeleton/01-02-SUMMARY.md
@README.md
@package.json
@src/config.ts
@src/cli.ts
</context>

<threat_model>
ASVS Level: 1
Block on: high severity threats.

- T-01-07: `.env.example` includes real credentials or concrete API keys. Mitigation: only empty provider/model placeholders are allowed, and `sk-` style secret strings are explicitly rejected.
- T-01-08: README implies model-provider support before it exists. Mitigation: README must include `## Phase 1 Behavior` and say the CLI response is a placeholder until Phase 2.
- T-01-09: Stale links point users back to the removed documentation directory. Mitigation: README and core `.planning` files must point to `.planning` artifacts only.

Verification references: Task 1 secret checks, Task 2 README behavior checks, Task 3 canonical planning checks, and `01-VALIDATION.md` documentation checks.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add environment example</name>
  <files>.env.example</files>
  <read_first>src/config.ts, .planning/phases/01-foundation-and-cli-skeleton/01-RESEARCH.md</read_first>
  <action>Create `.env.example` with comments and exact variables: `SMART_ASSISTANT_DATA_DIR=.smart-assistant`, `SMART_ASSISTANT_PROVIDER=`, and `SMART_ASSISTANT_MODEL=`. Include the comment `# Phase 1 does not call model providers yet; provider/model are reserved for Phase 2.` Do not include secrets or sample API keys.</action>
  <verify>test -f .env.example && rg \"SMART_ASSISTANT_DATA_DIR=.smart-assistant|SMART_ASSISTANT_PROVIDER=|SMART_ASSISTANT_MODEL=|Phase 1 does not call model providers\" .env.example</verify>
  <acceptance_criteria>
    - `.env.example` contains `SMART_ASSISTANT_DATA_DIR=.smart-assistant`.
    - `.env.example` contains `SMART_ASSISTANT_PROVIDER=`.
    - `.env.example` contains `SMART_ASSISTANT_MODEL=`.
    - `.env.example` contains `Phase 1 does not call model providers yet`.
    - `.env.example` does not contain `sk-` or any concrete API key value.
  </acceptance_criteria>
  <done>Environment variables are documented without leaking secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Update README quick start and local data sections</name>
  <files>README.md</files>
  <read_first>README.md, package.json, src/config.ts, src/cli.ts, .env.example</read_first>
  <action>Update `README.md` with a `## Quick Start` section containing exact commands: `npm install`, `npm run dev`, `npm run typecheck`, `npm run build`, and `node dist/cli.js --help`. Add a `## Local Data` section documenting default data directory `.smart-assistant`, override env var `SMART_ASSISTANT_DATA_DIR`, and subdirectories `sessions`, `memory`, `knowledge`, `plans`. Add a `## Phase 1 Behavior` section stating that the CLI returns a placeholder response and that Phase 2 wires `pi-agent-core` and `pi-ai`. Preserve the existing `.planning` navigation and do not add legacy documentation-directory links.</action>
  <verify>rg \"## Quick Start|npm run dev|npm run typecheck|node dist/cli.js --help|## Local Data|SMART_ASSISTANT_DATA_DIR|sessions|memory|knowledge|plans|## Phase 1 Behavior|pi-agent-core|pi-ai\" README.md && node -e \"const text=require('fs').readFileSync('README.md','utf8'); if (text.includes('docs' + '/')) process.exit(1)\"</verify>
  <acceptance_criteria>
    - `README.md` contains `## Quick Start`.
    - `README.md` contains `npm run dev`.
    - `README.md` contains `npm run typecheck`.
    - `README.md` contains `node dist/cli.js --help`.
    - `README.md` contains `## Local Data`.
    - `README.md` contains `SMART_ASSISTANT_DATA_DIR`.
    - `README.md` contains `sessions`, `memory`, `knowledge`, and `plans`.
    - `README.md` contains `## Phase 1 Behavior`.
    - `README.md` does not contain the stale legacy docs path.
  </acceptance_criteria>
  <done>README explains setup, local data, and Phase 1 limitations.</done>
</task>

<task type="auto">
  <name>Task 3: Verify planning surface stays consolidated</name>
  <files>README.md, .env.example</files>
  <read_first>README.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/research/SUMMARY.md</read_first>
  <action>Run a final documentation alignment pass. Ensure `README.md` points to `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/STATE.md`, and `.planning/research/SUMMARY.md`. Ensure the legacy documentation directory is not recreated and stale legacy documentation links do not remain in README or core `.planning` files. If a stale reference exists, replace it with the corresponding `.planning` artifact or remove it.</action>
  <verify>test ! -d docs && node -e \"const fs=require('fs'); const paths=['README.md','.planning/PROJECT.md','.planning/REQUIREMENTS.md','.planning/ROADMAP.md','.planning/STATE.md','.planning/research/SUMMARY.md']; const stale='docs' + '/'; for (const p of paths) if (fs.readFileSync(p,'utf8').includes(stale)) process.exit(1)\"</verify>
  <acceptance_criteria>
    - `README.md` contains `.planning/PROJECT.md`.
    - `README.md` contains `.planning/REQUIREMENTS.md`.
    - `README.md` contains `.planning/ROADMAP.md`.
    - `README.md` contains `.planning/STATE.md`.
    - `README.md` contains `.planning/research/SUMMARY.md`.
    - `test ! -d docs` exits 0.
    - Core planning files do not contain the stale legacy docs path.
  </acceptance_criteria>
  <done>Planning is consolidated in `.planning` and no stale docs links remain.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.env.example` exists and documents all Phase 1 env vars.
- [ ] `README.md` contains Quick Start, Local Data, and Phase 1 Behavior sections.
- [ ] `README.md` still links to `.planning` artifacts.
- [ ] `test ! -d docs` exits 0.
- [ ] Core planning files do not contain the stale legacy docs path.
</verification>

<success_criteria>

- User can understand how to install, run, typecheck, and build the CLI.
- User can understand where local data will be stored.
- README and `.planning` remain the single planning surface.
- Phase 1 limitations are explicit and do not imply model-provider support before Phase 2.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-cli-skeleton/01-03-SUMMARY.md`.
</output>
