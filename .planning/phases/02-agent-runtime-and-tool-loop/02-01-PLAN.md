---
phase: "smart-assistant-02-agent-runtime-and-tool-loop"
plan: "02-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
autonomous: true
requirements:
  - AGT-02
  - AGT-03
user_setup: []
must_haves:
  truths:
    - "Project has @earendil-works/pi-ai and @earendil-works/pi-agent-core as dependencies."
    - "TypeScript can import from @earendil-works/pi-ai and @earendil-works/pi-agent-core."
    - "Model registry can be accessed via getModel() from pi-ai."
  artifacts:
    - package.json
  key_links:
    - "package.json dependencies include @earendil-works/pi-ai and @earendil-works/pi-agent-core."
    - "TypeScript compilation succeeds with pi package imports."
---

<objective>
Add and verify pi-ai and pi-agent-core package dependencies.

Purpose: Establish the runtime integration points before wiring the agent loop. Verify that the monorepo packages can be imported and that the model registry is accessible.
Output: Updated package.json with pi dependencies and a successful compilation check.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md
@package.json
@tsconfig.json
</context>

<threat_model>
ASVS Level: 1
Block on: high severity threats.

- T-02-01: Packages are added from untrusted registries. Mitigation: only @earendil-works/pi-ai and @earendil-works/pi-agent-core from the local monorepo are allowed; verify the packages resolve from ../pi.
- T-02-02: Version drift causes runtime failures. Mitigation: use exact versions matching the local pi monorepo (^0.75.4 per RESEARCH.md).

Verification references: Task 1 dependency checks, Task 2 import verification.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add pi-ai and pi-agent-core dependencies</name>
  <files>package.json</files>
  <read_first>package.json, .planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md</read_first>
  <action>Update package.json dependencies to include "@earendil-works/pi-ai": "^0.75.4" and "@earendil-works/pi-agent-core": "^0.75.4". Also add "@sinclair/typebox": "^0.34.0" for tool parameter schemas. Preserve all existing devDependencies and scripts. Run `npm install` to verify the packages resolve from the local ../pi monorepo.</action>
  <verify>node -e "const p=require('./package.json'); if(!p.dependencies?.['@earendil-works/pi-ai']||!p.dependencies?.['@earendil-works/pi-agent-core']||!p.dependencies?.['@sinclair/typebox']) process.exit(1)"</verify>
  <acceptance_criteria>
    - `package.json` contains `"@earendil-works/pi-ai": "^0.75.4"` in dependencies.
    - `package.json` contains `"@earendil-works/pi-agent-core": "^0.75.4"` in dependencies.
    - `package.json` contains `"@sinclair/typebox": "^0.34.0"` in dependencies.
    - `npm install` exits 0.
    - `node_modules/@earendil-works/pi-ai` directory exists.
    - `node_modules/@earendil-works/pi-agent-core` directory exists.
  </acceptance_criteria>
  <done>Dependencies are installed and available in node_modules.</done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript can import pi packages</name>
  <files>src/index.ts</files>
  <read_first>src/index.ts, tsconfig.json, .planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md</read_first>
  <action>Create a temporary test file `src/pi-check.ts` that imports `getModel` from `@earendil-works/pi-ai` and `Agent` from `@earendil-works/pi-agent-core`. Call `getModel("anthropic", "claude-sonnet-4-20250514")` and export the result. Run `npm run typecheck` to verify TypeScript resolves the imports. After verification, delete `src/pi-check.ts`.</action>
  <verify>npm run typecheck && test ! -f src/pi-check.ts</verify>
  <acceptance_criteria>
    - TypeScript compilation succeeds with pi package imports.
    - `npm run typecheck` exits 0 after cleanup.
    - No residual test files remain in src/.
  </acceptance_criteria>
  <done>TypeScript can resolve and type-check imports from pi-ai and pi-agent-core.</done>
</task>

<task type="auto">
  <name>Task 3: Verify model registry access</name>
  <files>src/model.ts</files>
  <read_first>src/config.ts, .planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md</read_first>
  <action>Add a new file `src/model.ts` that imports `getModel` from `@earendil-works/pi-ai` and exports a function `getDefaultModel()` that returns `getModel("anthropic", "claude-sonnet-4-20250514")`. Add a JSDoc comment stating this is the default model for the assistant and can be overridden via environment variables in later phases. Export the function from `src/index.ts`.</action>
  <verify>test -f src/model.ts && rg "getModel.*anthropic.*claude-sonnet-4-20250514" src/model.ts && rg "export.*getDefaultModel" src/index.ts</verify>
  <acceptance_criteria>
    - `src/model.ts` exists.
    - `src/model.ts` imports `getModel` from `@earendil-works/pi-ai`.
    - `src/model.ts` exports `getDefaultModel` function.
    - `src/model.ts` contains `"anthropic"` and `"claude-sonnet-4-20250514"`.
    - `src/index.ts` exports `getDefaultModel`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Model registry is accessible and a default model is configured.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `package.json` has `@earendil-works/pi-ai`, `@earendil-works/pi-agent-core`, and `@sinclair/typebox` dependencies.
- [ ] `npm install` exits 0.
- [ ] `npm run typecheck` exits 0.
- [ ] `src/model.ts` exports `getDefaultModel()`.
- [ ] `src/index.ts` exports `getDefaultModel`.
</verification>

<success_criteria>

- pi-ai and pi-agent-core are installed and resolvable.
- TypeScript can import and type-check pi package APIs.
- Default model selection is centralized in a reusable module.
</success_criteria>

<output>
After completion, create `.planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-01-SUMMARY.md`.
</output>
