---
phase: "smart-assistant-02-agent-runtime-and-tool-loop"
plan: "02-02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/assistant/controller.ts
  - src/assistant/types.ts
  - src/assistant/index.ts
  - src/cli.ts
  - src/index.ts
autonomous: true
requirements:
  - AGT-01
  - AGT-02
  - AGT-06
user_setup: []
must_haves:
  truths:
    - "User can send a message from the CLI and receive an assistant response."
    - "Agent loop uses pi-agent-core for message state and event flow."
    - "Model calls go through pi-ai via the Agent class."
    - "Assistant behavior follows clarification and conservative failure rules."
  artifacts:
    - src/assistant/controller.ts
    - src/assistant/types.ts
    - src/cli.ts
  key_links:
    - "src/cli.ts calls assistant controller to process user messages."
    - "src/assistant/controller.ts creates Agent instance from pi-agent-core."
    - "Agent uses model from src/model.ts getDefaultModel()."
---

<objective>
Implement the Assistant Controller and wire the single agent runtime.

Purpose: Replace the CLI placeholder with a real agent loop using pi-agent-core. Connect user input to the agent and return responses through the event stream.
Output: A working assistant controller that can process user messages and return assistant responses.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md
@src/cli.ts
@src/config.ts
@src/model.ts
</context>

<threat_model>
ASVS Level: 1
Block on: high severity threats.

- T-02-03: Missing API key causes silent failure. Mitigation: controller must check for ANTHROPIC_API_KEY before creating agent and exit with clear error message if missing.
- T-02-04: Agent throws unhandled errors during streaming. Mitigation: subscribe to agent events and handle error events explicitly; do not let exceptions escape to CLI.
- T-02-05: User input is not validated before agent processing. Mitigation: trim whitespace and reject empty messages before calling agent.prompt().

Verification references: Task 1 API key check, Task 2 error handling, Task 3 input validation.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create Assistant Controller module structure</name>
  <files>src/assistant/types.ts, src/assistant/controller.ts, src/assistant/index.ts</files>
  <read_first>src/model.ts, .planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md, .planning/research/SUMMARY.md</read_first>
  <action>Create `src/assistant/types.ts` exporting `AssistantEvent` type with variants: `text_delta` (delta: string), `error` (message: string), `tool_start` (toolName: string), `tool_end` (toolName: string, isError: boolean). Create `src/assistant/controller.ts` importing `Agent`, `type AgentEvent`, `type AgentOptions` from `@earendil-works/pi-agent-core` and `getDefaultModel` from `../model.js`. Export an `AssistantController` class with a private `agent: Agent` field, a constructor that checks for `ANTHROPIC_API_KEY` (throws if missing), and initializes the Agent with system prompt "You are a helpful local assistant. When uncertain, ask clarifying questions. If you cannot answer confidently, say so." Create `src/assistant/index.ts` re-exporting from types and controller.</action>
  <verify>test -f src/assistant/types.ts && test -f src/assistant/controller.ts && test -f src/assistant/index.ts && rg "AssistantEvent|AssistantController|ANTHROPIC_API_KEY|You are a helpful local assistant" src/assistant/</verify>
  <acceptance_criteria>
    - `src/assistant/types.ts` exists.
    - `src/assistant/types.ts` exports `AssistantEvent` type with `text_delta`, `error`, `tool_start`, `tool_end` variants.
    - `src/assistant/controller.ts` exists.
    - `src/assistant/controller.ts` imports `Agent` from `@earendil-works/pi-agent-core`.
    - `src/assistant/controller.ts` checks for `ANTHROPIC_API_KEY` in constructor.
    - `src/assistant/controller.ts` throws error with message "ANTHROPIC_API_KEY environment variable is required" if key is missing.
    - `src/assistant/controller.ts` initializes Agent with system prompt containing "helpful local assistant".
    - `src/assistant/index.ts` exports `AssistantController` and `AssistantEvent`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Assistant controller module structure is in place with API key validation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement prompt method with event streaming</name>
  <files>src/assistant/controller.ts</files>
  <read_first>src/assistant/controller.ts, .planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md</read_first>
  <action>In `src/assistant/controller.ts`, add a `prompt(message: string, onEvent: (event: AssistantEvent) => void): Promise<void>` method. Validate that message is not empty after trimming; if empty, call onEvent with error event and return. Subscribe to agent events before calling `agent.prompt(message)`. In the subscription, handle `message_update` events: for `text_delta` type, call onEvent with `text_delta` event containing the delta string. Handle `message_end` events: if message has `errorMessage`, call onEvent with `error` event. Handle `tool_execution_start` and `tool_execution_end` events: call onEvent with corresponding `tool_start` and `tool_end` events. Use async/await and ensure the method resolves when agent.prompt() completes.</action>
  <verify>rg "prompt.*message.*onEvent|agent.prompt|message_update|text_delta|errorMessage|tool_execution_start" src/assistant/controller.ts</verify>
  <acceptance_criteria>
    - `src/assistant/controller.ts` contains `prompt(message: string, onEvent: (event: AssistantEvent) => void): Promise<void>`.
    - Method validates non-empty message and calls onEvent with error if empty.
    - Method calls `agent.subscribe()` before `agent.prompt()`.
    - Method handles `message_update` events and extracts `text_delta`.
    - Method handles `message_end` events and checks for `errorMessage`.
    - Method handles `tool_execution_start` events.
    - Method handles `tool_execution_end` events.
    - Method uses `await agent.prompt(message)`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Prompt method streams events from agent to callback.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CLI to Assistant Controller</name>
  <files>src/cli.ts</files>
  <read_first>src/cli.ts, src/assistant/controller.ts, src/config.ts</read_first>
  <action>Update `src/cli.ts` to import `AssistantController` from `./assistant/index.js`. In `runInteractive()`, create an `AssistantController` instance inside a try-catch block. If controller construction throws (missing API key), catch the error, print the message to stderr, and exit with code 1. Replace the placeholder response logic with a call to `controller.prompt(message, handleAssistantEvent)`. Implement `handleAssistantEvent(event)` to: for `text_delta`, write delta to stdout without newline; for `error`, write error message to stderr; for `tool_start`, write `[Calling tool: ${toolName}]` to stdout; for `tool_end`, write `[Tool ${toolName} completed]` to stdout if not error, or `[Tool ${toolName} failed]` if error. After prompt resolves, write a newline to stdout. Handle SIGINT to abort the agent if mid-stream.</action>
  <verify>rg "AssistantController|handleAssistantEvent|text_delta|Calling tool|Tool.*completed|Tool.*failed" src/cli.ts</verify>
  <acceptance_criteria>
    - `src/cli.ts` imports `AssistantController`.
    - `runInteractive()` creates `AssistantController` instance with try-catch.
    - API key error is caught and printed to stderr.
    - User messages are sent to `controller.prompt()`.
    - `handleAssistantEvent` function handles `text_delta`, `error`, `tool_start`, `tool_end`.
    - `text_delta` events write to stdout without newline.
    - `error` events write to stderr.
    - `tool_start` events write `[Calling tool: ...]` to stdout.
    - Newline is written after prompt completes.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>CLI is wired to the assistant controller and streams responses.</done>
</task>

<task type="auto">
  <name>Task 4: Export assistant module from index</name>
  <files>src/index.ts</files>
  <read_first>src/index.ts, src/assistant/index.ts</read_first>
  <action>Update `src/index.ts` to export all from `./assistant/index.js` in addition to existing exports.</action>
  <verify>rg "export \* from \"./assistant/index.js\"" src/index.ts</verify>
  <acceptance_criteria>
    - `src/index.ts` contains `export * from "./assistant/index.js"`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Assistant module is exported for programmatic use.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `src/assistant/controller.ts` exists and exports `AssistantController`.
- [ ] `AssistantController` constructor checks for `ANTHROPIC_API_KEY`.
- [ ] `AssistantController` has `prompt(message, onEvent)` method.
- [ ] `src/cli.ts` uses `AssistantController` instead of placeholder.
- [ ] `npm run typecheck` exits 0.
- [ ] `npm run build` exits 0.
</verification>

<success_criteria>

- User can send a message from CLI and receive a streaming assistant response.
- Agent loop uses pi-agent-core for message state and event flow.
- Model calls go through pi-ai via the Agent class.
- Missing API key produces a clear error message.
- Empty messages are rejected with an error event.
</success_criteria>

<output>
After completion, create `.planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-02-SUMMARY.md`.
</output>
