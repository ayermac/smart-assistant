---
wave: 2
depends_on: [03-01]
files_modified:
  - src/assistant/controller.ts
  - src/assistant/types.ts
autonomous: true
requirements:
  - SES-01
  - SES-03
---

# Plan 03-02: Integrate Session Persistence into AssistantController

## Goal

Modify AssistantController to accept initial messages for session restore, subscribe to agent events once in the constructor, and persist messages after each agent turn.

## Tasks

### Task 1: Update AssistantController constructor

<read_first>
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/assistant/controller.ts (current implementation)
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/session/types.ts (SessionStore interface)
- /Users/chenchao/Workspace/agent-learn/smart-assistant/.planning/phases/03-local-session-persistence/03-RESEARCH.md (Agent constructor with initialState, subscription bug fix)
</read_first>

<action>
Modify `src/assistant/controller.ts`:

1. Import `SessionStore` type from `../session/types.js`
2. Add constructor parameters: `initialMessages: AgentMessage[]`, `sessionStore: SessionStore`, `sessionId: string`
3. Pass `initialMessages` to Agent's `initialState.messages` (replace empty array)
4. Store `sessionStore` and `sessionId` as private readonly fields
5. Move `this.agent.subscribe()` call from `prompt()` method to constructor (fixes duplicate subscription bug)
6. Store subscription handler reference for potential cleanup
7. Update JSDoc to document new constructor parameters

Constructor signature: `constructor(initialMessages: AgentMessage[], sessionStore: SessionStore, sessionId: string)`
</action>

<acceptance_criteria>
- Constructor accepts exactly 3 parameters: initialMessages, sessionStore, sessionId
- Agent is initialized with `initialState.messages` set to `initialMessages` parameter
- `sessionStore` and `sessionId` are stored as private fields
- `this.agent.subscribe()` is called exactly once in constructor, not in `prompt()`
- TypeScript compilation passes: `pnpm tsc --noEmit` returns exit code 0
</acceptance_criteria>

### Task 2: Add agent_end event persistence

<read_first>
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/assistant/controller.ts (modified in Task 1)
- /Users/chenchao/Workspace/agent-learn/smart-assistant/.planning/phases/03-local-session-persistence/03-RESEARCH.md (agent_end event structure)
</read_first>

<action>
Modify `src/assistant/controller.ts` constructor's subscription handler:

1. Add case for `agent_end` event type in the existing switch statement inside the subscription handler
2. When `event.type === "agent_end"`, call `this.sessionStore.save(this.sessionId, event.messages)`
3. Use `void` operator to explicitly ignore the promise (fire-and-forget pattern): `void this.sessionStore.save(...)`
4. Do NOT await the save operation to avoid blocking the agent loop

The subscription handler should now handle both existing events (message_update, message_end, tool_execution_start, tool_execution_end) and the new agent_end event.
</action>

<acceptance_criteria>
- Constructor subscription handler includes case for `agent_end` event
- When `agent_end` fires, `sessionStore.save()` is called with sessionId and messages
- Save operation uses `void` operator (not awaited)
- TypeScript compilation passes: `pnpm tsc --noEmit` returns exit code 0
</acceptance_criteria>

### Task 3: Add getMessages getter method

<read_first>
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/assistant/controller.ts (modified in Task 2)
</read_first>

<action>
Add public getter method to AssistantController class:

1. Method name: `getMessages()`
2. Return type: `AgentMessage[]`
3. Implementation: return copy of messages array: `return [...this.agent.state.messages]`
4. Add JSDoc: `Get a copy of the current message array.`

This enables explicit save on exit if needed in the future.
</action>

<acceptance_criteria>
- Method `getMessages()` exists on AssistantController class
- Method returns spread copy of `this.agent.state.messages`
- Return type is `AgentMessage[]`
- JSDoc comment present
- TypeScript compilation passes: `pnpm tsc --noEmit` returns exit code 0
</acceptance_criteria>

## Verification

After completing all tasks:

1. Run `pnpm tsc --noEmit` — must pass with exit code 0
2. Verify no TypeScript errors about AgentMessage or SessionStore types
3. Verify constructor signature matches expected interface

## must_haves

- [ ] Constructor accepts initialMessages, sessionStore, and sessionId parameters
- [ ] Agent initialized with restored messages
- [ ] Subscription moved to constructor (bug fix)
- [ ] agent_end event triggers session save
- [ ] getMessages() method returns current messages
