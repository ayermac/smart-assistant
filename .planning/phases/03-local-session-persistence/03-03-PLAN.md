---
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/cli.ts
autonomous: true
requirements:
  - SES-02
  - SES-01
  - SES-03
---

# Plan 03-03: Add CLI Arguments for Session Resume

## Goal

Update CLI to parse `--session <id>` and `--new` flags, implement session resolution logic (resume latest or specific session), and wire session loading into startup flow.

## Tasks

### Task 1: Add session-related CLI options

<read_first>
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/cli.ts (current argument parsing)
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/config.ts (resolveDataPaths pattern)
</read_first>

<action>
Modify `src/cli.ts`:

1. Update `CliOptions` type to include: `sessionId?: string`, `newSession?: boolean`
2. Update `USAGE` string to document new options:
   - Add `--session <id>  Resume specific session`
   - Add `--new            Start a new session (don't resume latest)`
3. In `parseArgs()` function, add handling for `--session` flag:
   - If flag present, validate next argument exists and doesn't start with `-`
   - Store value in `options.sessionId`, increment index
4. In `parseArgs()` function, add handling for `--new` flag:
   - Set `options.newSession = true`, no value needed
5. Add validation: if both `--session` and `--new` are provided, return error kind with message `"Cannot use --session and --new together"`
</action>

<acceptance_criteria>
- `CliOptions` type has `sessionId?: string` and `newSession?: boolean` fields
- `USAGE` string documents `--session <id>` and `--new` options
- `parseArgs()` correctly parses `--session 2026-05-22T00-30-00` and sets `sessionId`
- `parseArgs()` correctly parses `--new` and sets `newSession = true`
- Error returned when both `--session` and `--new` are provided together
- TypeScript compilation passes: `pnpm tsc --noEmit` returns exit code 0
</acceptance_criteria>

### Task 2: Implement session resolution logic

<read_first>
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/cli.ts (modified in Task 1)
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/session/index.ts (FileSessionStore)
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/config.ts (resolveDataPaths)
</read_first>

<action>
Modify `src/cli.ts`:

1. Import `FileSessionStore` from `./session/index.js`
2. Import `resolveDataPaths` from `./config.js`
3. Create new async function `resolveSession(options: CliOptions, store: FileSessionStore): Promise<{ session: SessionFile; isNew: boolean }>`
4. Implement resolution logic in this order:
   a. If `options.sessionId` is set: load session by ID, throw error if not found, return `{ session, isNew: false }`
   b. If `options.newSession` is true: create new session, return `{ session: store.create(), isNew: true }`
   c. Default (neither flag): get latest session
      - If latest exists: load and return `{ session, isNew: false }`
      - If no sessions exist: create new session, return `{ session: store.create(), isNew: true }`

Import `SessionFile` type from `./session/index.js` for return type.
</action>

<acceptance_criteria>
- Function `resolveSession()` exists and is exported or used internally
- Returns object with `session` (SessionFile) and `isNew` (boolean) properties
- When `sessionId` provided: loads that session or throws error
- When `newSession` true: creates new session
- When neither flag: loads latest or creates new if none exist
- TypeScript compilation passes: `pnpm tsc --noEmit` returns exit code 0
</acceptance_criteria>

### Task 3: Wire session into CLI startup

<read_first>
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/cli.ts (modified in Task 2)
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/assistant/controller.ts (new constructor signature from 03-02)
</read_first>

<action>
Modify `runInteractive()` function in `src/cli.ts`:

1. Get sessions directory from `resolveDataPaths(process.env).sessions`
2. Create `FileSessionStore` instance with sessions directory
3. Call `resolveSession(options, store)` to get session and isNew flag (use await)
4. Log session status to user:
   - If `isNew` is true: write `"New session: {sessionId}\n"`
   - If `isNew` is false: write `"Resumed session: {sessionId} ({messageCount} messages)\n"`
5. Pass `session.messages`, `store`, and `session.id` to AssistantController constructor (replacing the old no-args constructor)
6. Update the catch block to show session-related errors clearly
</action>

<acceptance_criteria>
- `FileSessionStore` is instantiated with sessions directory
- `resolveSession()` is called before creating AssistantController
- Session status message printed (new or resumed with message count)
- AssistantController created with 3 arguments: `session.messages`, `store`, `session.id`
- CLI starts successfully and shows session info
- TypeScript compilation passes: `pnpm tsc --noEmit` returns exit code 0
</acceptance_criteria>

### Task 4: Update CLI startup output

<read_first>
- /Users/chenchao/Workspace/agent-learn/smart-assistant/src/cli.ts (modified in Task 3)
</read_first>

<action>
Modify `runInteractive()` function to improve startup output:

1. Keep existing "smart-assistant local CLI" line
2. Keep existing "Data dir: {dataDir}" line
3. Move session info line (new/resumed) to appear after data dir line
4. Keep existing "Type /help for commands" line at the end

Output order:
```
smart-assistant local CLI
Data dir: {dataDir}
[New session: {sessionId}] or [Resumed session: {sessionId} (N messages)]
Set SMART_ASSISTANT_DATA_DIR or pass --data-dir to change it.
Type /help for commands.
```
</action>

<acceptance_criteria>
- Startup output shows exactly 5 lines in correct order
- Session line shows "New session: {id}" for new sessions
- Session line shows "Resumed session: {id} (N messages)" for existing sessions
- Message count matches actual messages in restored session
- TypeScript compilation passes: `pnpm tsc --noEmit` returns exit code 0
</acceptance_criteria>

## Verification

After completing all tasks:

1. Run `pnpm tsc --noEmit` — must pass with exit code 0
2. Run `pnpm build` — must complete successfully
3. Test CLI manually:
   - `pnpm start` (should create new session if none exist)
   - `pnpm start` again (should resume latest session)
   - `pnpm start -- --new` (should create new session)
   - `pnpm start -- --session 2026-05-22T00-30-00` (should resume specific or error)
4. Verify session files are created in `.smart-assistant/sessions/`
5. Verify messages persist across CLI restarts

## must_haves

- [ ] CLI parses `--session <id>` and `--new` flags
- [ ] Session resolution implements correct priority (explicit ID > --new > default latest)
- [ ] AssistantController initialized with restored messages
- [ ] Session status displayed at startup (new vs resumed)
- [ ] Sessions persist and can be resumed
