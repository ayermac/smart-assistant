---
phase: "smart-assistant-02-agent-runtime-and-tool-loop"
plan: "02-03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/tools/get_time.ts
  - src/tools/registry.ts
  - src/tools/index.ts
  - src/assistant/controller.ts
autonomous: true
requirements:
  - AGT-05
  - TLS-01
  - TLS-02
  - TLS-03
user_setup: []
must_haves:
  truths:
    - "Tool registry exposes get_time tool with stable input/output contract."
    - "Assistant can call get_time tool and include the result in response."
    - "Tool implementation is local and does not require cloud services."
    - "Tool returns success, empty result, or explainable error."
  artifacts:
    - src/tools/get_time.ts
    - src/tools/registry.ts
  key_links:
    - "src/tools/get_time.ts exports AgentTool conforming to pi-agent-core contract."
    - "src/tools/registry.ts exports array of all available tools."
    - "src/assistant/controller.ts imports tools from registry."
---

<objective>
Implement tool registry and get_time tool contract.

Purpose: Create the first local tool that demonstrates tool calling capability. Establish the tool registry pattern for future tools (remember, recall_memory, search_knowledge, etc.).
Output: A working get_time tool that can be called by the assistant and returns current time information.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md
@src/assistant/controller.ts
</context>

<threat_model>
ASVS Level: 1
Block on: high severity threats.

- T-02-06: Tool executes arbitrary code or shell commands. Mitigation: get_time only uses Node.js Date API; no shell execution or eval.
- T-02-07: Tool leaks sensitive system information. Mitigation: get_time only returns timezone and timestamp; no paths, usernames, or environment variables.
- T-02-08: Invalid timezone input causes unhandled exception. Mitigation: wrap timezone conversion in try-catch and return error message in tool result.

Verification references: Task 1 input validation, Task 2 error handling, Task 3 registry integration.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create get_time tool implementation</name>
  <files>src/tools/get_time.ts</files>
  <read_first>.planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md, src/model.ts</read_first>
  <action>Create `src/tools/get_time.ts` importing `Type` from `@sinclair/typebox` and `type AgentTool` from `@earendil-works/pi-agent-core`. Export a `get_time` constant typed as `AgentTool` with: name "get_time", description "Get the current date and time. Optionally specify a timezone.", label "Get Current Time". Define parameters schema using Type.Object with optional timezone field (Type.Optional(Type.String with description "IANA timezone identifier like America/New_York. Defaults to UTC.")). In execute function, accept toolCallId, params, signal, onUpdate. Extract timezone from params (default "UTC"). Create new Date(). Wrap toLocaleString call in try-catch with dateStyle "full" and timeStyle "long". On success, return object with content array containing type "text" and text "Current time in ${tz}: ${timeStr}", and details object with timezone and iso timestamp. On invalid timezone error, return content with error message explaining valid format.</action>
  <verify>test -f src/tools/get_time.ts && rg "AgentTool|get_time|Type.Object|timezone|toLocaleString|IANA" src/tools/get_time.ts</verify>
  <acceptance_criteria>
    - `src/tools/get_time.ts` exists.
    - File imports `Type` from `@sinclair/typebox`.
    - File imports `AgentTool` from `@earendil-works/pi-agent-core`.
    - Tool name is exactly `"get_time"`.
    - Tool description mentions "current date and time".
    - Tool has label "Get Current Time".
    - Parameters schema uses `Type.Object` with optional `timezone` field.
    - Execute function uses `Date` and `toLocaleString`.
    - Execute function wraps timezone conversion in try-catch.
    - On success, returns content with time string.
    - On error, returns content with error message about invalid timezone.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>get_time tool is implemented with TypeBox schema and error handling.</done>
</task>

<task type="auto">
  <name>Task 2: Create tool registry</name>
  <files>src/tools/registry.ts, src/tools/index.ts</files>
  <read_first>src/tools/get_time.ts, .planning/REQUIREMENTS.md</read_first>
  <action>Create `src/tools/registry.ts` importing `get_time` from `./get_time.js` and `type AgentTool` from `@earendil-works/pi-agent-core`. Export a constant `ALL_TOOLS: AgentTool[]` containing `[get_time]`. Add a JSDoc comment: "Registry of all available tools for the assistant. Phase 2 includes get_time only. Future phases will add remember, recall_memory, search_knowledge, create_plan, update_plan." Create `src/tools/index.ts` that re-exports `get_time`, `ALL_TOOLS`, and the `AgentTool` type.</action>
  <verify>test -f src/tools/registry.ts && test -f src/tools/index.ts && rg "ALL_TOOLS|get_time|AgentTool" src/tools/</verify>
  <acceptance_criteria>
    - `src/tools/registry.ts` exists.
    - `src/tools/registry.ts` imports `get_time` from `./get_time.js`.
    - `src/tools/registry.ts` exports `ALL_TOOLS` array.
    - `ALL_TOOLS` contains `get_time`.
    - `src/tools/index.ts` exists.
    - `src/tools/index.ts` re-exports `get_time`, `ALL_TOOLS`, and `AgentTool`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Tool registry is established with single get_time tool.</done>
</task>

<task type="auto">
  <name>Task 3: Register tools with Assistant Controller</name>
  <files>src/assistant/controller.ts</files>
  <read_first>src/assistant/controller.ts, src/tools/registry.ts, .planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md</read_first>
  <action>Update `src/assistant/controller.ts` to import `ALL_TOOLS` from `../tools/index.js`. In the Agent initialization within the constructor, add `tools: ALL_TOOLS` to the initialState object. The Agent class from pi-agent-core will automatically handle tool calling when the model requests it.</action>
  <verify>rg "import.*ALL_TOOLS|tools.*ALL_TOOLS" src/assistant/controller.ts</verify>
  <acceptance_criteria>
    - `src/assistant/controller.ts` imports `ALL_TOOLS` from `../tools/index.js`.
    - Agent initialState includes `tools: ALL_TOOLS`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Tools are registered with the agent and available for model use.</done>
</task>

<task type="auto">
  <name>Task 4: Export tools module from main index</name>
  <files>src/index.ts</files>
  <read_first>src/index.ts, src/tools/index.ts</read_first>
  <action>Update `src/index.ts` to export all from `./tools/index.js` in addition to existing exports.</action>
  <verify>rg "export \* from \"./tools/index.js\"" src/index.ts</verify>
  <acceptance_criteria>
    - `src/index.ts` contains `export * from "./tools/index.js"`.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Tools module is exported for programmatic use.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `src/tools/get_time.ts` exists with TypeBox schema.
- [ ] `src/tools/registry.ts` exports `ALL_TOOLS` array.
- [ ] `src/assistant/controller.ts` imports and uses `ALL_TOOLS`.
- [ ] `npm run typecheck` exits 0.
- [ ] `npm run build` exits 0.
</verification>

<success_criteria>

- Tool registry exposes get_time tool.
- Tool has stable input/output contract with TypeBox schema.
- Tool implementation is local (no cloud services).
- Tool can be called by the assistant and return results.
- Tool handles errors gracefully and returns explainable messages.
</success_criteria>

<output>
After completion, create `.planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-03-SUMMARY.md`.
</output>
