---
phase: "smart-assistant-02-agent-runtime-and-tool-loop"
plan: "02-04"
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - src/cli.ts
  - src/assistant/controller.ts
  - README.md
autonomous: true
requirements:
  - AGT-04
  - AGT-06
user_setup: []
must_haves:
  truths:
    - "CLI shows streamed response text output character by character."
    - "Tool execution status is visible during tool calls."
    - "Errors are handled gracefully without crashing the CLI."
    - "SIGINT (Ctrl+C) aborts the current agent operation."
  artifacts:
    - src/cli.ts
    - src/assistant/controller.ts
  key_links:
    - "src/cli.ts handleAssistantEvent streams text deltas to stdout."
    - "src/cli.ts catches errors and prints to stderr."
    - "src/assistant/controller.ts handles agent error events."
---

<objective>
Add streaming CLI output and conservative failure handling.

Purpose: Ensure the CLI provides real-time feedback during agent responses and handles errors gracefully without crashing.
Output: A polished CLI experience with streaming output, visible tool status, and robust error handling.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md
@src/cli.ts
@src/assistant/controller.ts
</context>

<threat_model>
ASVS Level: 1
Block on: high severity threats.

- T-02-09: SIGINT during streaming leaves agent in inconsistent state. Mitigation: call agent.abort() on SIGINT and ensure clean exit.
- T-02-10: Error during streaming causes unhandled promise rejection. Mitigation: wrap prompt() call in try-catch and print error to stderr.
- T-02-11: Empty or null content in stream causes undefined output. Mitigation: validate event content before writing to stdout.

Verification references: Task 1 SIGINT handling, Task 2 error handling, Task 3 content validation.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add SIGINT handler for graceful abort</name>
  <files>src/cli.ts, src/assistant/controller.ts</files>
  <read_first>src/cli.ts, src/assistant/controller.ts, .planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md</read_first>
  <action>Update `src/assistant/controller.ts` to add an `abort()` method that calls `this.agent.abort()`. Update `src/cli.ts` to store the controller instance in a variable accessible to a SIGINT handler. In `runInteractive()`, register a one-time SIGINT handler using `process.once("SIGINT", ...)` that calls `controller.abort()`, writes "\n[Aborted]\n" to stdout, and exits with code 0. The handler should be registered before the prompt loop and removed after each prompt completes. Use a flag to track if a prompt is in progress.</action>
  <verify>rg "abort\(\)|SIGINT|Aborted|isPromptInProgress" src/cli.ts src/assistant/controller.ts</verify>
  <acceptance_criteria>
    - `src/assistant/controller.ts` has `abort()` method.
    - `abort()` method calls `this.agent.abort()`.
    - `src/cli.ts` registers SIGINT handler.
    - SIGINT handler calls `controller.abort()`.
    - SIGINT handler writes "[Aborted]" to stdout.
    - CLI tracks prompt-in-progress state with a flag.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>SIGINT gracefully aborts ongoing agent operations.</done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive error handling wrapper</name>
  <files>src/cli.ts</files>
  <read_first>src/cli.ts, .planning/research/SUMMARY.md</read_first>
  <action>Update `src/cli.ts` to wrap the `controller.prompt()` call in a try-catch block. Catch any errors, write the error message to stderr with format "\n[Error: ${error.message}]\n", and continue the prompt loop (do not exit). Ensure the prompt-in-progress flag is reset in a finally block. Handle the case where the error is unknown (not an Error instance) by converting to string.</action>
  <verify>rg "try.*catch|Error:|stderr|finally|isPromptInProgress = false" src/cli.ts</verify>
  <acceptance_criteria>
    - `controller.prompt()` call is wrapped in try-catch.
    - Errors are written to stderr.
    - Error output includes "[Error: ...]" format.
    - Non-Error exceptions are converted to string safely.
    - Prompt-in-progress flag is reset in finally block.
    - Prompt loop continues after error (does not exit).
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>All errors are caught and displayed without crashing the CLI.</done>
</task>

<task type="auto">
  <name>Task 3: Improve streaming output formatting</name>
  <files>src/cli.ts</files>
  <read_first>src/cli.ts, .planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md</read_first>
  <action>Update the `handleAssistantEvent` function in `src/cli.ts` to improve output formatting: for `text_delta`, validate delta is a non-empty string before writing; for `tool_start`, write "\n[Tool: ${toolName}]" on a new line; for `tool_end`, write " done\n" if successful or " failed\n" if error; for `error`, write "\n[Error: ${message}]\n" to stderr. Add color codes using ANSI escape sequences: green (\x1b[32m) for tool success, red (\x1b[31m) for errors, reset (\x1b[0m) after each colored output. Write "assistant> " prefix before the first text delta of each response using a flag to track first delta.</action>
  <verify>rg "\\x1b\\[32m|\\x1b\\[31m|\\x1b\\[0m|assistant>|Tool:|done|failed" src/cli.ts</verify>
  <acceptance_criteria>
    - `handleAssistantEvent` validates delta is non-empty before writing.
    - `tool_start` writes "[Tool: ${toolName}]" on new line.
    - `tool_end` writes "done" for success.
    - `tool_end` writes "failed" for errors.
    - Errors use red ANSI color (\x1b[31m).
    - Success uses green ANSI color (\x1b[32m).
    - Reset code (\x1b[0m) follows colored output.
    - First text delta is prefixed with "assistant> ".
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>Streaming output is well-formatted with colors and clear status indicators.</done>
</task>

<task type="auto">
  <name>Task 4: Update README with Phase 2 capabilities</name>
  <files>README.md</files>
  <read_first>README.md, .planning/REQUIREMENTS.md, .planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-RESEARCH.md</read_first>
  <action>Update `README.md` to replace the Phase 1 Behavior section with a Phase 2 Status section. Document that the CLI now connects to the assistant runtime using pi-agent-core and pi-ai. Document that ANTHROPIC_API_KEY environment variable is required. Document that streaming responses are shown in real-time. Document that the get_time tool is available. Add instructions for setting ANTHROPIC_API_KEY in the environment or .env file. Update the Quick Start section to mention the API key requirement. Preserve existing sections and links to .planning artifacts.</action>
  <verify>rg "ANTHROPIC_API_KEY|pi-agent-core|pi-ai|get_time|streaming" README.md</verify>
  <acceptance_criteria>
    - `README.md` contains `ANTHROPIC_API_KEY`.
    - `README.md` mentions `pi-agent-core` and `pi-ai`.
    - `README.md` mentions streaming responses.
    - `README.md` mentions `get_time` tool.
    - Quick Start mentions API key requirement.
    - Phase 1 Behavior section is replaced or updated.
    - Links to .planning artifacts are preserved.
  </acceptance_criteria>
  <done>README reflects Phase 2 capabilities and setup requirements.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `src/cli.ts` handles SIGINT gracefully.
- [ ] `src/cli.ts` catches errors during prompt and displays them.
- [ ] `src/cli.ts` formats streaming output with colors and status.
- [ ] `README.md` documents Phase 2 capabilities and API key requirement.
- [ ] `npm run typecheck` exits 0.
- [ ] `npm run build` exits 0.
</verification>

<success_criteria>

- CLI shows streamed response text output in real-time.
- Tool execution status is visible with clear indicators.
- Errors are handled gracefully without crashing.
- SIGINT aborts current operation cleanly.
- README accurately documents the current capabilities.
</success_criteria>

<output>
After completion, create `.planning/phases/smart-assistant-02-agent-runtime-and-tool-loop/02-04-SUMMARY.md`.
</output>
