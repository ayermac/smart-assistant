---
wave: 1
depends_on: []
files_modified:
  - src/config.ts
  - src/knowledge/types.ts
  - src/knowledge/index.ts
  - .env.example
autonomous: true
---

# Plan 05-01: Knowledge Directory Configuration and File Discovery

**Goal:** Add configuration for knowledge source directory and define all knowledge interfaces.

**Requirements:** RAG-01, RAG-06 (partial)

## Tasks

### Task 1: Add knowledge directory configuration to config.ts

<read_first>
- src/config.ts
- .env.example
</read_first>

<action>
Add SMART_ASSISTANT_KNOWLEDGE_DIR_ENV constant ("SMART_ASSISTANT_KNOWLEDGE_DIR") and resolveKnowledgeSourceDir function to src/config.ts. The function checks SMART_ASSISTANT_KNOWLEDGE_DIR env var, falling back to join(resolveDataDir(env), "knowledge-sources"). Add documentation to .env.example for the new environment variable.
</action>

<acceptance_criteria>
- src/config.ts exports SMART_ASSISTANT_KNOWLEDGE_DIR_ENV with value "SMART_ASSISTANT_KNOWLEDGE_DIR"
- src/config.ts exports resolveKnowledgeSourceDir function returning string
- resolveKnowledgeSourceDir returns env var value when set, otherwise join(dataDir, "knowledge-sources")
- .env.example contains SMART_ASSISTANT_KNOWLEDGE_DIR with description comment
- pnpm tsc --noEmit passes
</acceptance_criteria>

### Task 2: Create knowledge types.ts with core interfaces

<read_first>
- src/memory/types.ts (pattern reference for interface style)
- src/config.ts
</read_first>

<action>
Create src/knowledge/types.ts. Define KnowledgeChunk interface with fields: id (string), sourcePath (string), headingLevel (number), headingText (string), text (string), startLine (number), endLine (number), tags (string[]), createdAt (string). Define KnowledgeMatch with chunk, relevanceScore (number), matchReason (string). Define SearchOptions with optional limit, tags, sourcePath. Define ChunkMetadata with id, sourcePath, headingText, headingLevel, tags, lineCount, charCount, modifiedAt. Define SourceMetadata with path, absolutePath, mtime, chunkCount. Define KnowledgeManifest with version (1), lastIndexed, sourceDir, chunks (ChunkMetadata[]), sources (SourceMetadata[]). Define KnowledgeStore interface with ingest, search, getManifest, needsReindex, getChunk, listChunks methods. Follow the export style from src/memory/types.ts.
</action>

<acceptance_criteria>
- src/knowledge/types.ts exports KnowledgeChunk with all 9 fields
- src/knowledge/types.ts exports KnowledgeMatch, SearchOptions, KnowledgeManifest
- src/knowledge/types.ts exports ChunkMetadata and SourceMetadata
- src/knowledge/types.ts exports KnowledgeStore interface with 6 methods
- pnpm tsc --noEmit passes
</acceptance_criteria>

### Task 3: Create knowledge module index.ts barrel export

<read_first>
- src/memory/index.ts (pattern reference)
- src/knowledge/types.ts
</read_first>

<action>
Create src/knowledge/index.ts that re-exports all types from types.ts. Follow the export style from src/memory/index.ts (type exports first, then value exports).
</action>

<acceptance_criteria>
- src/knowledge/index.ts exists and re-exports all types from types.ts
- pnpm tsc --noEmit passes
</acceptance_criteria>

## Verification

```bash
pnpm tsc --noEmit
```

## must_haves

- Knowledge directory configurable via SMART_ASSISTANT_KNOWLEDGE_DIR environment variable
- Default knowledge directory is {dataDir}/knowledge-sources
- All knowledge interfaces defined and exported (KnowledgeChunk, KnowledgeStore, KnowledgeManifest, etc.)
- TypeScript compiles without errors
