---
wave: 4
depends_on:
  - 05-03
files_modified:
  - src/tools/registry.ts
  - src/tools/index.ts
  - src/assistant/controller.ts
autonomous: true
---

# Plan 05-04: Integrate RAG Context and Empty-Result Behavior

**Goal:** Wire FileKnowledgeStore into assistant controller, register search_knowledge tool, and update system prompt with knowledge behavior rules.

**Requirements:** RAG-01, RAG-05, RAG-06 (final)

## Tasks

### Task 1: Extend createAllTools to accept KnowledgeStore

<read_first>
- src/tools/registry.ts
- src/tools/knowledge.ts
- src/knowledge/types.ts
</read_first>

<action>
Update createAllTools in src/tools/registry.ts to accept optional second parameter knowledgeStore of type KnowledgeStore. Import createSearchKnowledgeTool from knowledge.js. When knowledgeStore is provided, include createSearchKnowledgeTool(knowledgeStore) in returned array. Keep backward compatibility: if knowledgeStore is undefined, omit the tool.
</action>

<acceptance_criteria>
- createAllTools signature: (memoryStore: MemoryStore, knowledgeStore?: KnowledgeStore) => AgentTool[]
- When knowledgeStore provided, search_knowledge tool included in returned array
- When knowledgeStore undefined, search_knowledge omitted
- get_time, remember, recall_memory remain in array
- pnpm tsc --noEmit passes
</acceptance_criteria>

### Task 2: Update assistant controller to initialize KnowledgeStore and update system prompt

<read_first>
- src/assistant/controller.ts
- src/knowledge/store.ts
- src/tools/registry.ts
</read_first>

<action>
In AssistantController constructor: import FileKnowledgeStore from knowledge module. Create FileKnowledgeStore instance after FileMemoryStore (around line 72). Pass both stores to createAllTools(memoryStore, knowledgeStore). Update SYSTEM_PROMPT: add paragraph after memory rules about search_knowledge tool — instruct to use search_knowledge when user asks about notes/documents, cite source paths using format "According to `path > heading`...", state explicitly when knowledge base lacks relevant information, distinguish memory vs knowledge search when relevant.
</action>

<acceptance_criteria>
- AssistantController imports FileKnowledgeStore
- FileKnowledgeStore instance created in constructor (after FileMemoryStore)
- createAllTools called with both memoryStore and knowledgeStore
- SYSTEM_PROMPT includes search_knowledge behavior rules
- SYSTEM_PROMPT instructs citing source paths (e.g., "According to `path > heading`...")
- SYSTEM_PROMPT instructs stating explicitly when knowledge base lacks answer
- SYSTEM_PROMPT distinguishes memory vs knowledge search
- pnpm tsc --noEmit passes
- pnpm build succeeds
</acceptance_criteria>

## Verification

```bash
pnpm tsc --noEmit && pnpm build
```

## must_haves

- FileKnowledgeStore initialized in assistant controller (RAG-01)
- search_knowledge tool registered and available to the agent
- System prompt includes knowledge behavior rules with citation format
- System prompt instructs explicit statement when knowledge base does not contain answer (RAG-05)
- Memory and knowledge clearly distinguished in system prompt (MEM-05 continuation)
- TypeScript compiles without errors
- Build succeeds
