---
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/assistant/controller.ts
  - src/tools/knowledge.ts
requirements:
  - RAG2-03
  - RAG2-04
autonomous: true
---

# Plan 08-02: Wire VectorKnowledgeStore into Assistant

## Objective

Update the assistant controller to use `VectorKnowledgeStore` for knowledge search, enabling semantic retrieval for RAG queries.

## Tasks

### Task 1: Update assistant controller to use VectorKnowledgeStore

<read_first>
- src/assistant/controller.ts
- src/knowledge/vector-store.ts
- src/config.ts
</read_first>

<action>
Update `src/assistant/controller.ts`:
1. Import `VectorKnowledgeStore` from knowledge module
2. Import `createDefaultEmbeddingConfig` from memory module
3. Initialize `VectorKnowledgeStore` with embedding config
4. Call `init()` during assistant initialization (after memory store init)
5. Use as primary knowledge store
6. Add try-catch for graceful error handling
</action>

<acceptance_criteria>
- `VectorKnowledgeStore` imported and initialized
- `init()` called during startup
- Knowledge search uses vector store
- Error handling prevents crashes on initialization failure
- Build and typecheck pass
</acceptance_criteria>

### Task 2: Verify knowledge tool compatibility

<read_first>
- src/tools/knowledge.ts
</read_first>

<action>
Ensure `search_knowledge` tool works with `VectorKnowledgeStore`:
1. Tool interface unchanged (works with any KnowledgeStore)
2. Verify source citation still works
3. Verify empty result handling
4. No changes needed if tool uses store interface correctly
</action>

<acceptance_criteria>
- `search_knowledge` tool works with `VectorKnowledgeStore`
- Source paths correctly cited in responses
- Empty results handled gracefully
- No breaking changes to tool interface
</acceptance_criteria>

### Task 3: Test semantic search capabilities

<read_first>
- src/knowledge/vector-store.ts
</read_first>

<action>
Manual test to verify semantic search works:
1. Add test documents to `.smart-assistant/knowledge-sources/`
2. Run assistant and trigger knowledge ingestion
3. Test queries that would fail with keyword matching but succeed with semantic search:
   - "身份认证" should match "authentication"
   - "性能优化" should match "performance tuning"
   - "数据库设计" should match "database schema"
4. Verify results ranked by relevance score
</action>

<acceptance_criteria>
- Semantic queries return relevant results
- Vector similarity scores correctly calculated
- Results ranked by relevance (highest first)
- Cross-language matching works (Chinese ↔ English)
</acceptance_criteria>

## Verification

```bash
npm run build
npm run typecheck
npm run dev  # Manual test
```

## Notes

- Semantic search enables matching without exact keywords
- Same embedding model as Memory (Doubao doubao-embedding-vision)
- Vector search is more expensive than keyword matching (API call per query)
- Consider caching query embeddings in future (deferred)
