---
wave: 3
depends_on:
  - 08-02
files_modified:
  - src/assistant/controller.ts
  - .env.example
requirements:
  - RAG2-04
autonomous: true
---

# Plan 08-03: Update assistant controller to use VectorMemoryStore

## Objective

Integrate VectorMemoryStore into the assistant controller, replacing FileMemoryStore for memory operations.

## Tasks

### Task 1: Update controller to use VectorMemoryStore

<read_first>
- src/assistant/controller.ts
- src/memory/vector-store.ts
- src/config.ts
</read_first>

<action>
Update `src/assistant/controller.ts`:
1. Import `VectorMemoryStore` from '../memory/index.js'
2. Replace `FileMemoryStore` initialization with `VectorMemoryStore`
3. Create embedding config using environment variables
4. Initialize VectorMemoryStore with config
5. Call `await memoryStore.init()` during startup
</action>

<acceptance_criteria>
- `src/assistant/controller.ts` imports VectorMemoryStore
- FileMemoryStore is no longer used
- VectorMemoryStore is initialized with embedding config
- `init()` is called during assistant startup
- Memory tools (remember, recall_memory) use VectorMemoryStore
</acceptance_criteria>

### Task 2: Update environment configuration documentation

<read_first>
- .env.example
- README.md
</read_first>

<action>
1. Ensure `.env.example` includes all embedding configuration:
   - `ANTHROPIC_API_KEY` (existing)
   - `EMBEDDING_BASE_URL` with default
   - `EMBEDDING_MODEL` with default
2. Update README.md to document the new embedding configuration
3. Add note about ChromaDB data storage location
</action>

<acceptance_criteria>
- `.env.example` is complete with all embedding variables
- README.md explains embedding configuration
- Documentation mentions ChromaDB storage path
</acceptance_criteria>

### Task 3: Test semantic retrieval

<read_first>
- src/assistant/controller.ts
</read_first>

<action>
Test the complete flow:
1. Start assistant with `npm run dev`
2. Say "记住你叫小C" to store a memory
3. Ask "你是谁" and verify memory is retrieved
4. Ask "你叫什么名字" and verify memory is retrieved
5. Ask "谁" and verify memory is retrieved
</action>

<acceptance_criteria>
- Memory is stored successfully via VectorMemoryStore
- Query "你是谁" retrieves "你叫小C" memory
- Query "你叫什么名字" retrieves memory
- Query "谁" retrieves memory
- No hardcoded synonyms are used
</acceptance_criteria>

## Verification

```bash
npm run build
npm run typecheck
npm run dev
```

## Notes

- This plan replaces the synonym-based matching entirely
- Existing JSON memories remain but are not migrated yet (Phase 9)
- The synonym expansion code in FileMemoryStore can be removed in Phase 9
